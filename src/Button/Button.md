Наша любимая красная кнопка:

```ts
<Button>Push Me</Button>
```

Тут красную кнопку нельзя редактировать:

```jsx noeditor
<Button>Push Me</Button>
```

Вот так обычно мы испортируем реакт:

```jsx static
import React from 'react';
```

Stay tuned, так сказать.
